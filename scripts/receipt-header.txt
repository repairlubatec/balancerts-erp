#e6edf5] bg-white p-3"><div className="mb-2 flex flex-wrap items-center justify-between gap-2"><div className="flex items-center gap-3 rounded border border-[#e6edf5] bg-[#fbfcfd] p-2 text-[11px] text-slate-600">{company.logoUrl ? <img src={company.logoUrl} alt={`Logótipo de ${company.name ?? "empresa"}`} className="h-10 w-10 object-contain" onError={(event) => { event.currentTarget.hidden = true; }} /> : null}<div><strong className="text-[#102a43]">{company.name ?? "Empresa activa"}</strong> · Recibo interno de conferência<br />Período seleccionado: {formatInternalReceiptPeriod((payrollRuns.data ?? []).find(({ run }) => run.id === selectedPayrollRunId)?.run)} · Emitido em {new Date().toLocaleDateString("pt-PT")}</div></div><div className="flex flex-wrap items-center gap-2"><p className="text-xs font-semibold text-[#102a43]">Recibos internos e mapa detalhado</p><select value={selectedReceiptEmployeeId} onChange={(event) => setSelectedReceiptEmployeeId(event.target.value)} aria-label="Seleccionar colaborador para recibo individual" className="h-8 rounded border border-[#dbe5f1] bg-white px-2 text-xs"><option value="">Mapa colectivo ({selectedPayrollItems.length})</option>{selectedPayrollItems.map(({ employee }) => <option key={employee.id} value={employee.id}>{employee.employeeNumber} · {employee.fullName}</option>)}</select></div><div className="grid w-full gap-2 border-y border-[#dbe5f1] py-2 text-[11px] text-slate-600 md:grid-cols-3"><div><strong className="text-[#102a43]">Responsável pelo cálculo</strong><br />{(() => { const selected = filteredPayrollRuns.find(({ run }) => run.id === selectedPayrollRunId); return formatPayrollActor(selected?.actors?.creator, selected?.run.createdAt, "Não registado"); })()}</div><div><strong className="text-[#102a43]">Conferente</strong><br />{(() => { const selected = filteredPayrollRuns.find(({ run }) => run.id === selectedPayrollRunId); return formatPayrollActor(selected?.actors?.reviewer, selected?.run.reviewedAt, "Ainda não conferida"); })()}</div><div><strong className="text-[#102a43]">Aprovador</strong><br />{(() => { const selected = filteredPayrollRuns.find(({ run }) => run.id === selectedPayrollRunId); return formatPayrollActor(selected?.actors?.approver, selected?.run.approvedAt, "Ainda não aprovada"); })()}</div></div><div className="flex gap-1 print:hidden"><Button type="button" size="sm" variant="outline" onClick={() => exportPayroll("csv")}>CSV</Button><Button type="button" size="sm" variant="outline" onClick={() => exportPayroll("xlsx")}>XLSX</Button><Button type="button" size="sm" variant="outline" onClick={printInternalReceipt}>Imprimir / guardar PDF</Button></div></div><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="border-b bg-[#f8fafd] text-[10px] uppercase tracking-[0.12em] text-slate-500"><tr><th className="px-3 py-2">Colaborador</th><th className="px-3 py-2">Bruto</th><th className="px-3 py-2">Segurança Social</th><th className="px-3 py-2">IRT</th><th className="px-3 py-2">Líquido</th></tr></thead><tbody className="divide-y divide-[#edf2f7]">{receiptPayrollItems.map(({ item, employee }) => <tr key={item.id}><td className="px-3 py-2 font-medium text-slate-700">{employee.fullName}</td><td className="px-3 py-2">{Number(item.grossAmount).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2">{Number(item.socialEmployeeAmount).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2">{Number(item.irtAmount).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2 font-semibold">{Number(item.netAmount).toLocaleString("pt-PT")} AOA</td></tr>)}</tbody></table></div><p className="mt-2 text-[11px] text-slate-500">Este detalhe é interno, auditado e não substitui recibo oficial nem declaração submetida.</p><div className="mt-5 grid gap-8 border-t border-[#dbe5f1] pt-4 text-xs md:grid-cols-2"><div><p className="font-semibold text-[#102a43]">Conferência interna</p><p className="mt-5 border-b border-slate-400 pb-1">Nome: ________