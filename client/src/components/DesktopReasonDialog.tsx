import React from "react";
import { Button } from "@/components/ui/button";
import { DesktopWindowFrame } from "@/components/DesktopWindowFrame";

export function DesktopReasonDialog({ open, title, subtitle, description, value, onChange, onConfirm, onCancel, pending, confirmLabel = "Confirmar" }: { open: boolean; title: string; subtitle: string; description: string; value: string; onChange: (value: string) => void; onConfirm: () => void; onCancel: () => void; pending?: boolean; confirmLabel?: string }) {
  if (!open) return null;
  return <div className="fixed inset-0 z-[80] flex items-center justify-center bg-[#0b1f33]/35 p-4" role="presentation"><div className="w-full max-w-lg" role="dialog" aria-modal="true" aria-labelledby="desktop-reason-dialog-title"><DesktopWindowFrame title={title} subtitle={subtitle} onClose={pending ? undefined : onCancel}><form onSubmit={(event) => { event.preventDefault(); onConfirm(); }} className="space-y-4 bg-[#f7f9fb] p-4"><div className="rounded border border-[#dbe5f1] bg-white p-3 text-xs text-[#30465d]"><p id="desktop-reason-dialog-title" className="font-semibold text-[#102a43]">{description}</p></div><label className="block text-xs font-semibold text-[#30465d]" htmlFor="desktop-reason-input">Motivo obrigatório<input id="desktop-reason-input" autoFocus value={value} onChange={(event) => onChange(event.target.value)} maxLength={500} placeholder="Escreva o motivo" className="mt-1 h-9 w-full rounded border border-[#aebdcb] bg-white px-3 text-sm font-normal outline-none focus:border-[#1267d6] focus:ring-2 focus:ring-[#1267d6]/20" /></label><div className="flex justify-end gap-2 border-t border-[#dbe5f1] pt-3"><Button type="button" variant="outline" disabled={pending} onClick={onCancel} className="bg-white">Cancelar</Button><Button type="submit" disabled={pending || !value.trim()} className="bg-[#1267d6] hover:bg-[#0f58b8]">{pending ? "A processar…" : confirmLabel}</Button></div></form></DesktopWindowFrame></div></div>;
}
