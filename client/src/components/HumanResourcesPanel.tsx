import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { trpc } from "@/lib/trpc";
import { userFacingError } from "@/lib/presentationLabels";
import { cn } from "@/lib/utils";

type Props = { company?: { id: number; organizationId: number; name?: string } };

const statusLabel: Record<string, string> = { ACTIVE: "Activo", INACTIVE: "Inactivo", SUSPENDED: "Suspenso", DRAFT: "Rascunho", ENDED: "Terminado", CANCELLED: "Cancelado" };
const contractTypeLabel: Record<string, string> = { INDETERMINADO: "Tempo indeterminado", TERMO: "Termo certo", TEMPO_PARCIAL: "Tempo parcial", ESTAGIO: "Estágio", PRESTACAO_SERVICOS: "Prestação de serviços" };

function StateBadge({ value }: { value: string }) {
  const positive = ["ACTIVE"].includes(value);
  const negative = ["SUSPENDED", "CANCELLED"].includes(value);
  return <Badge className={cn("border", positive ? "border-emerald-200 bg-emerald-50 text-emerald-700" : negative ? "border-rose-200 bg-rose-50 text-rose-700" : "border-amber-200 bg-amber-50 text-amber-700")}>{statusLabel[value] ?? value}</Badge>;
}

export function HumanResourcesPanel({ company }: Props) {
  const companyId = company?.id;
  const organizationId = company?.organizationId;
  const utils = trpc.useUtils();
  const employees = trpc.humanResources.employees.useQuery({ companyId: companyId ?? 0 }, { enabled: Boolean(companyId) });
  const contracts = trpc.humanResources.contracts.useQuery({ companyId: companyId ?? 0 }, { enabled: Boolean(companyId) });
  const payrollRules = trpc.humanResources.payrollRules.useQuery({ companyId: companyId ?? 0 }, { enabled: Boolean(companyId) });
  const payrollRuns = trpc.humanResources.payrollRuns.useQuery({ companyId: companyId ?? 0 }, { enabled: Boolean(companyId) });
  const [selectedPayrollRunId, setSelectedPayrollRunId] = useState<number | null>(null);
  const payrollItems = trpc.humanResources.payrollItems.useQuery({ companyId: companyId ?? 0, runId: selectedPayrollRunId ?? 0 }, { enabled: Boolean(companyId && selectedPayrollRunId) });
  const [employeeForm, setEmployeeForm] = useState({ employeeNumber: "", fullName: "", taxId: "", socialSecurityNumber: "", hireDate: new Date().toISOString().slice(0, 10), email: "", phone: "", position: "" });
  const [contractForm, setContractForm] = useState({ employeeId: "", contractNumber: "", contractType: "INDETERMINADO" as "INDETERMINADO" | "TERMO" | "TEMPO_PARCIAL" | "ESTAGIO" | "PRESTACAO_SERVICOS", position: "", department: "", startDate: new Date().toISOString().slice(0, 10), endDate: "", baseSalary: "", weeklyHours: "44" });
  const [feedback, setFeedback] = useState<string | null>(null);
  const createEmployee = trpc.humanResources.createEmployee.useMutation({ onSuccess: async () => { setFeedback("Colaborador criado e registado no trilho de auditoria."); setEmployeeForm((current) => ({ ...current, employeeNumber: "", fullName: "", taxId: "", socialSecurityNumber: "", email: "", phone: "", position: "" })); await employees.refetch(); }, onError: (error) => setFeedback(`Não foi possível criar o colaborador: ${userFacingError(error.message)}`) });
  const createContract = trpc.humanResources.createContract.useMutation({ onSuccess: async () => { setFeedback("Contrato criado em rascunho. Reveja e active-o após conferência."); setContractForm((current) => ({ ...current, employeeId: "", contractNumber: "", position: "", department: "", endDate: "", baseSalary: "" })); await contracts.refetch(); }, onError: (error) => setFeedback(`Não foi possível criar o contrato: ${userFacingError(error.message)}`) });
  const transitionContract = trpc.humanResources.transitionContract.useMutation({ onSuccess: async () => { setFeedback("Estado do contrato actualizado e auditado."); await contracts.refetch(); }, onError: (error) => setFeedback(`Não foi possível actualizar o contrato: ${userFacingError(error.message)}`) });
  const [payrollForm, setPayrollForm] = useState({ ruleSetId: "", year: String(new Date().getFullYear()), month: String(new Date().getMonth() + 1) });
  const [ruleForm, setRuleForm] = useState({ version: "", effectiveFrom: new Date().toISOString().slice(0, 10), employeeRate: "", employerRate: "", brackets: '[{"upTo":100000,"rate":0},{"upTo":null,"rate":0}]', sourceUrl: "" });
  const createPayrollRule = trpc.humanResources.createPayrollRule.useMutation({ onSuccess: async () => { setFeedback("Regra salarial criada em revisão interna; confirme a fonte oficial antes de usar."); setRuleForm((current) => ({ ...current, version: "" })); await payrollRules.refetch(); }, onError: (error) => setFeedback(`Não foi possível criar a regra salarial: ${userFacingError(error.message)}`) });
  const calculatePayroll = trpc.humanResources.calculatePayroll.useMutation({ onSuccess: async () => { setFeedback("Folha calculada e guardada em estado calculado; requer revisão antes de lançamento."); await payrollRuns.refetch(); }, onError: (error) => setFeedback(`Não foi possível calcular a folha: ${userFacingError(error.message)}`) });
  const approvePayroll = trpc.humanResources.approvePayroll.useMutation({ onSuccess: async () => { setFeedback("Folha aprovada e pronta para fecho."); await payrollRuns.refetch(); }, onError: (error) => setFeedback(`Não foi possível aprovar a folha: ${userFacingError(error.message)}`) });
  const closePayroll = trpc.humanResources.closePayroll.useMutation({ onSuccess: async () => { setFeedback("Folha fechada e protegida contra alterações; ligação contabilística preparada."); await payrollRuns.refetch(); }, onError: (error) => setFeedback(`Não foi possível fechar a folha: ${userFacingError(error.message)}`) });
  if (!companyId || !organizationId) return <Card className="border-amber-200 bg-amber-50"><CardContent className="p-5 text-sm text-amber-900">Seleccione uma empresa activa para trabalhar no módulo de Recursos Humanos.</CardContent></Card>;
  const updateEmployee = (key: keyof typeof employeeForm) => (event: React.ChangeEvent<HTMLInputElement>) => setEmployeeForm((current) => ({ ...current, [key]: event.target.value }));
  const updateContract = (key: keyof typeof contractForm) => (event: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => setContractForm((current) => ({ ...current, [key]: event.target.value }));
  const submitEmployee = (event: React.FormEvent) => { event.preventDefault(); if (!employeeForm.employeeNumber.trim() || !employeeForm.fullName.trim() || !employeeForm.position.trim()) return setFeedback("Indique número, nome e função do colaborador."); createEmployee.mutate({ organizationId, companyId, employeeNumber: employeeForm.employeeNumber.trim(), fullName: employeeForm.fullName.trim(), taxId: employeeForm.taxId.trim() || undefined, socialSecurityNumber: employeeForm.socialSecurityNumber.trim() || undefined, hireDate: new Date(employeeForm.hireDate), email: employeeForm.email.trim() || undefined, phone: employeeForm.phone.trim() || undefined }); };
  const submitContract = (event: React.FormEvent) => { event.preventDefault(); const salary = Number(contractForm.baseSalary); if (!Number(contractForm.employeeId) || !contractForm.contractNumber.trim() || !contractForm.position.trim() || !Number.isFinite(salary) || salary < 0) return setFeedback("Preencha colaborador, número, função e salário base válido."); createContract.mutate({ organizationId, companyId, employeeId: Number(contractForm.employeeId), contractNumber: contractForm.contractNumber.trim(), contractType: contractForm.contractType, position: contractForm.position.trim(), department: contractForm.department.trim() || undefined, startDate: new Date(contractForm.startDate), endDate: contractForm.endDate ? new Date(contractForm.endDate) : undefined, baseSalary: salary, weeklyHours: Number(contractForm.weeklyHours) || 44 }); };
  return <div className="space-y-3"><Card className="rounded-sm border-[#bfc9d4] bg-[#fbfcfd] shadow-none"><CardHeader className="border-b border-[#e6edf5] pb-3"><div className="flex items-start justify-between gap-3"><div><CardTitle className="text-base text-[#102a43]">Recursos Humanos</CardTitle><p className="mt-1 text-xs text-slate-500">Colaboradores e contratos da empresa activa · {company.name ?? "empresa seleccionada"}</p></div><Badge className="border-blue-200 bg-blue-50 text-blue-700">Dados isolados por empresa</Badge></div></CardHeader><CardContent className="p-3"><Tabs defaultValue="colaboradores"><TabsList className="mb-3"><TabsTrigger value="colaboradores">Colaboradores ({employees.data?.length ?? 0})</TabsTrigger><TabsTrigger value="contratos">Contratos ({contracts.data?.length ?? 0})</TabsTrigger><TabsTrigger value="folha">Folha ({payrollRuns.data?.length ?? 0})</TabsTrigger></TabsList><TabsContent value="colaboradores" className="space-y-3"><form onSubmit={submitEmployee} className="grid gap-2 rounded-md border border-[#e6edf5] bg-white p-3 md:grid-cols-4"><Input value={employeeForm.employeeNumber} onChange={updateEmployee("employeeNumber")} placeholder="Número interno" aria-label="Número interno" /><Input value={employeeForm.fullName} onChange={updateEmployee("fullName")} placeholder="Nome completo" aria-label="Nome completo" /><Input value={employeeForm.position} onChange={updateEmployee("position")} placeholder="Função inicial" aria-label="Função inicial" /><Input type="date" value={employeeForm.hireDate} onChange={updateEmployee("hireDate")} aria-label="Data de admissão" /><Input value={employeeForm.taxId} onChange={updateEmployee("taxId")} placeholder="NIF" aria-label="NIF" /><Input value={employeeForm.socialSecurityNumber} onChange={updateEmployee("socialSecurityNumber")} placeholder="Número INSS" aria-label="Número INSS" /><Input type="email" value={employeeForm.email} onChange={updateEmployee("email")} placeholder="Email" aria-label="Email" /><Input value={employeeForm.phone} onChange={updateEmployee("phone")} placeholder="Telefone" aria-label="Telefone" /><div className="flex justify-end md:col-span-4"><Button type="submit" disabled={createEmployee.isPending} className="bg-[#1267d6] hover:bg-[#0f58b8]">{createEmployee.isPending ? "A criar…" : "Adicionar colaborador"}</Button></div></form><div className="overflow-x-auto rounded-md border border-[#e6edf5]"><table className="w-full text-left text-xs"><thead className="border-b bg-[#f8fafd] text-[10px] uppercase tracking-[0.12em] text-slate-500"><tr><th className="px-3 py-2">Número</th><th className="px-3 py-2">Nome</th><th className="px-3 py-2">NIF</th><th className="px-3 py-2">Admissão</th><th className="px-3 py-2">Estado</th></tr></thead><tbody className="divide-y divide-[#edf2f7]">{(employees.data ?? []).map(({ employee }) => <tr key={employee.id}><td className="px-3 py-2 font-semibold text-[#1267d6]">{employee.employeeNumber}</td><td className="px-3 py-2 text-slate-700">{employee.fullName}</td><td className="px-3 py-2 text-slate-600">{employee.taxId ?? "—"}</td><td className="px-3 py-2 text-slate-600">{new Date(employee.hireDate).toLocaleDateString("pt-PT")}</td><td className="px-3 py-2"><StateBadge value={employee.status} /></td></tr>)}{!employees.data?.length && <tr><td colSpan={5} className="px-3 py-8 text-center text-slate-500">Ainda não existem colaboradores nesta empresa.</td></tr>}</tbody></table></div></TabsContent><TabsContent value="contratos" className="space-y-3"><form onSubmit={submitContract} className="grid gap-2 rounded-md border border-[#e6edf5] bg-white p-3 md:grid-cols-4"><select value={contractForm.employeeId} onChange={updateContract("employeeId")} aria-label="Colaborador" className="h-9 rounded-md border border-[#dbe5f1] bg-white px-2 text-sm"><option value="">Colaborador</option>{(employees.data ?? []).map(({ employee }) => <option key={employee.id} value={employee.id}>{employee.employeeNumber} · {employee.fullName}</option>)}</select><Input value={contractForm.contractNumber} onChange={updateContract("contractNumber")} placeholder="Número do contrato" aria-label="Número do contrato" /><select value={contractForm.contractType} onChange={updateContract("contractType")} aria-label="Tipo de contrato" className="h-9 rounded-md border border-[#dbe5f1] bg-white px-2 text-sm">{Object.entries(contractTypeLabel).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select><Input value={contractForm.position} onChange={updateContract("position")} placeholder="Função" aria-label="Função" /><Input value={contractForm.department} onChange={updateContract("department")} placeholder="Departamento" aria-label="Departamento" /><Input type="date" value={contractForm.startDate} onChange={updateContract("startDate")} aria-label="Início do contrato" /><Input type="date" value={contractForm.endDate} onChange={updateContract("endDate")} aria-label="Fim do contrato" /><Input type="number" min="0" step="0.01" value={contractForm.baseSalary} onChange={updateContract("baseSalary")} placeholder="Salário base AOA" aria-label="Salário base AOA" /><Input type="number" min="1" step="0.01" value={contractForm.weeklyHours} onChange={updateContract("weeklyHours")} placeholder="Horas semanais" aria-label="Horas semanais" /><div className="flex justify-end md:col-span-4"><Button type="submit" disabled={createContract.isPending} className="bg-[#477514] hover:bg-[#3b6410]">{createContract.isPending ? "A guardar…" : "Criar contrato em rascunho"}</Button></div></form><div className="overflow-x-auto rounded-md border border-[#e6edf5]"><table className="w-full text-left text-xs"><thead className="border-b bg-[#f8fafd] text-[10px] uppercase tracking-[0.12em] text-slate-500"><tr><th className="px-3 py-2">Contrato</th><th className="px-3 py-2">Colaborador</th><th className="px-3 py-2">Tipo</th><th className="px-3 py-2">Salário base</th><th className="px-3 py-2">Estado</th><th className="px-3 py-2">Acção</th></tr></thead><tbody className="divide-y divide-[#edf2f7]">{(contracts.data ?? []).map(({ contract, employee }) => <tr key={contract.id}><td className="px-3 py-2 font-semibold text-[#1267d6]">{contract.contractNumber}</td><td className="px-3 py-2 text-slate-700">{employee.fullName}</td><td className="px-3 py-2 text-slate-600">{contractTypeLabel[contract.contractType] ?? contract.contractType}</td><td className="px-3 py-2 text-slate-600">{Number(contract.baseSalary).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2"><StateBadge value={contract.status} /></td><td className="px-3 py-2"><div className="flex gap-1">{contract.status === "DRAFT" && <Button type="button" size="sm" variant="outline" onClick={() => transitionContract.mutate({ companyId, contractId: contract.id, to: "ACTIVE" })}>Activar</Button>}{contract.status === "ACTIVE" && <Button type="button" size="sm" variant="outline" onClick={() => transitionContract.mutate({ companyId, contractId: contract.id, to: "ENDED", terminationReason: "Cessação registada no posto RH" })}>Terminar</Button>}</div></td></tr>)}{!contracts.data?.length && <tr><td colSpan={6} className="px-3 py-8 text-center text-slate-500">Ainda não existem contratos nesta empresa.</td></tr>}</tbody></table></div></TabsContent><TabsContent value="folha" className="space-y-3"><div className="rounded-md border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900">A folha usa regras IRT e Segurança Social versionadas. O cálculo fica em revisão interna e não submete declarações à AGT ou ao INSS.</div><form onSubmit={(event) => { event.preventDefault(); if (!ruleForm.version.trim() || !ruleForm.employeeRate || !ruleForm.employerRate) return setFeedback("Indique versão e taxas da regra salarial."); createPayrollRule.mutate({ organizationId, companyId, version: ruleForm.version.trim(), effectiveFrom: new Date(ruleForm.effectiveFrom), socialEmployeeRate: Number(ruleForm.employeeRate), socialEmployerRate: Number(ruleForm.employerRate), irtBrackets: ruleForm.brackets, sourceUrl: ruleForm.sourceUrl.trim() || undefined }); }} className="grid gap-2 rounded-md border border-[#e6edf5] bg-white p-3 md:grid-cols-5"><Input value={ruleForm.version} onChange={(event) => setRuleForm((current) => ({ ...current, version: event.target.value }))} placeholder="Versão da regra" aria-label="Versão da regra" /><Input type="date" value={ruleForm.effectiveFrom} onChange={(event) => setRuleForm((current) => ({ ...current, effectiveFrom: event.target.value }))} aria-label="Vigência inicial" /><Input type="number" min="0" max="100" step="0.0001" value={ruleForm.employeeRate} onChange={(event) => setRuleForm((current) => ({ ...current, employeeRate: event.target.value }))} placeholder="INSS trabalhador %" aria-label="Taxa trabalhador" /><Input type="number" min="0" max="100" step="0.0001" value={ruleForm.employerRate} onChange={(event) => setRuleForm((current) => ({ ...current, employerRate: event.target.value }))} placeholder="INSS empregador %" aria-label="Taxa empregador" /><Button type="submit" disabled={createPayrollRule.isPending} className="bg-[#477514] hover:bg-[#3b6410]">{createPayrollRule.isPending ? "A guardar…" : "Guardar regra"}</Button><Input value={ruleForm.sourceUrl} onChange={(event) => setRuleForm((current) => ({ ...current, sourceUrl: event.target.value }))} placeholder="Fonte oficial (URL)" aria-label="Fonte oficial" className="md:col-span-2" /><textarea value={ruleForm.brackets} onChange={(event) => setRuleForm((current) => ({ ...current, brackets: event.target.value }))} aria-label="Tabela IRT em JSON" className="min-h-20 rounded-md border border-[#dbe5f1] bg-white px-3 py-2 font-mono text-xs md:col-span-3" /></form><div className="grid gap-2 rounded-md border border-[#e6edf5] bg-white p-3 md:grid-cols-4"><select value={payrollForm.ruleSetId} onChange={(event) => setPayrollForm((current) => ({ ...current, ruleSetId: event.target.value }))} aria-label="Regra salarial" className="h-9 rounded-md border border-[#dbe5f1] bg-white px-2 text-sm"><option value="">Regra salarial</option>{(payrollRules.data ?? []).map(({ ruleSet }) => <option key={ruleSet.id} value={ruleSet.id}>{ruleSet.version} · {ruleSet.verificationStatus === "INTERNAL_REVIEW" ? "Revisão interna" : "Verificada"}</option>)}</select><Input type="number" min="2000" max="2100" value={payrollForm.year} onChange={(event) => setPayrollForm((current) => ({ ...current, year: event.target.value }))} placeholder="Ano" aria-label="Ano da folha" /><Input type="number" min="1" max="12" value={payrollForm.month} onChange={(event) => setPayrollForm((current) => ({ ...current, month: event.target.value }))} placeholder="Mês" aria-label="Mês da folha" /><Button type="button" disabled={calculatePayroll.isPending || !payrollForm.ruleSetId} onClick={() => calculatePayroll.mutate({ organizationId, companyId, ruleSetId: Number(payrollForm.ruleSetId), year: Number(payrollForm.year), month: Number(payrollForm.month) })} className="bg-[#1267d6] hover:bg-[#0f58b8]">{calculatePayroll.isPending ? "A calcular…" : "Calcular folha"}</Button></div><div className="overflow-x-auto rounded-md border border-[#e6edf5]"><table className="w-full text-left text-xs"><thead className="border-b bg-[#f8fafd] text-[10px] uppercase tracking-[0.12em] text-slate-500"><tr><th className="px-3 py-2">Período</th><th className="px-3 py-2">Regra</th><th className="px-3 py-2">Bruto</th><th className="px-3 py-2">IRT</th><th className="px-3 py-2">Líquido</th><th className="px-3 py-2">Estado</th><th className="px-3 py-2">Acção</th></tr></thead><tbody className="divide-y divide-[#edf2f7]">{(payrollRuns.data ?? []).map(({ run, ruleSet }) => <tr key={run.id} onClick={() => setSelectedPayrollRunId(run.id)} className={cn("cursor-pointer hover:bg-[#f7faff]", selectedPayrollRunId === run.id && "bg-[#f0f6ff]")}><td className="px-3 py-2 font-semibold text-[#1267d6]">{String(run.month).padStart(2, "0")}/{run.year}</td><td className="px-3 py-2 text-slate-700">{ruleSet.version}</td><td className="px-3 py-2 text-slate-600">{Number(run.grossTotal).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2 text-slate-600">{Number(run.irtTotal).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2 text-slate-600">{Number(run.netTotal).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2"><StateBadge value={run.status} /></td><td className="px-3 py-2"><div className="flex gap-1">{run.status === "CALCULATED" && <Button type="button" size="sm" variant="outline" onClick={(event) => { event.stopPropagation(); approvePayroll.mutate({ companyId, runId: run.id }); }}>Aprovar</Button>}{run.status === "APPROVED" && <Button type="button" size="sm" variant="outline" onClick={(event) => { event.stopPropagation(); closePayroll.mutate({ companyId, runId: run.id }); }}>Fechar</Button>}</div></td></tr>)}{!payrollRuns.data?.length && <tr><td colSpan={7} className="px-3 py-8 text-center text-slate-500">Ainda não existem folhas calculadas nesta empresa.</td></tr>}</tbody></table></div>{selectedPayrollRunId && <div className="rounded-md border border-[#e6edf5] bg-white p-3"><p className="mb-2 text-xs font-semibold text-[#102a43]">Recibos internos e mapa detalhado</p><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="border-b bg-[#f8fafd] text-[10px] uppercase tracking-[0.12em] text-slate-500"><tr><th className="px-3 py-2">Colaborador</th><th className="px-3 py-2">Bruto</th><th className="px-3 py-2">Segurança Social</th><th className="px-3 py-2">IRT</th><th className="px-3 py-2">Líquido</th></tr></thead><tbody className="divide-y divide-[#edf2f7]">{(payrollItems.data ?? []).map(({ item, employee }) => <tr key={item.id}><td className="px-3 py-2 font-medium text-slate-700">{employee.fullName}</td><td className="px-3 py-2">{Number(item.grossAmount).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2">{Number(item.socialEmployeeAmount).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2">{Number(item.irtAmount).toLocaleString("pt-PT")} AOA</td><td className="px-3 py-2 font-semibold">{Number(item.netAmount).toLocaleString("pt-PT")} AOA</td></tr>)}</tbody></table></div><p className="mt-2 text-[11px] text-slate-500">Este detalhe é interno, auditado e não substitui recibo oficial nem declaração submetida.</p></div>}</TabsContent></Tabs>{feedback && <p role="status" className="mt-3 text-xs font-medium text-[#477514]">{feedback}</p>}</CardContent></Card></div>;
}
