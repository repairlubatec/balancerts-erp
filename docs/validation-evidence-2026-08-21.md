# Evidência de validação — 21/08/2026

## Estado técnico

A suite global terminou com **98 ficheiros de teste aprovados e 366 testes aprovados**. A verificação `pnpm exec tsc --noEmit` terminou sem erros.

## Pendências abertas

O `todo.md` contém **20 itens ainda abertos**. A classificação indica que estes itens exigem pelo menos um dos seguintes elementos: credenciais oficiais AGT ou bancárias, endpoint externo, máquina Windows limpa, destino MySQL/TiDB isolado, utilizadores e dados de aceitação da Repair Lubatec, ou confirmação integral de fontes normativas PGCA/IVA.

## Regra de entrega

Nenhum dos itens acima deve ser marcado como concluído apenas por existir um adaptador ou uma preparação local. A activação de uma integração externa, a homologação fiscal, o restauro real, a assinatura de código e a aprovação normativa exigem evidência operacional correspondente.
