CREATE TABLE `saadiAlerts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int NOT NULL,
	`studyId` int NOT NULL,
	`code` varchar(80) NOT NULL,
	`severity` enum('CRITICO','ATENCAO','FAVORAVEL') NOT NULL,
	`title` varchar(180) NOT NULL,
	`description` text NOT NULL,
	`thresholdValue` varchar(40),
	`actualValue` varchar(40),
	`resolved` int NOT NULL DEFAULT 0,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `saadiAlerts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `saadiAssumptions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int,
	`studyId` int NOT NULL,
	`scenarioId` int,
	`name` varchar(120) NOT NULL,
	`value` varchar(80) NOT NULL,
	`unit` varchar(40) NOT NULL,
	`startYear` int NOT NULL,
	`endYear` int NOT NULL,
	`source` varchar(255),
	`notes` text,
	`dataOrigin` enum('MANUAL','ERP','DOCUMENTO','IA') NOT NULL DEFAULT 'MANUAL',
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `saadiAssumptions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `saadiExternalCompanies` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`studyId` int NOT NULL,
	`legalName` varchar(180) NOT NULL,
	`nif` varchar(32),
	`societyType` varchar(80),
	`registrationNumber` varchar(80),
	`incorporationDate` timestamp,
	`activity` varchar(180),
	`activityCode` varchar(40),
	`sector` varchar(120),
	`country` varchar(80) NOT NULL DEFAULT 'Angola',
	`province` varchar(120),
	`municipality` varchar(120),
	`address` varchar(255),
	`phone` varchar(40),
	`email` varchar(320),
	`website` varchar(255),
	`contactName` varchar(180),
	`contactPosition` varchar(120),
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `saadiExternalCompanies_id` PRIMARY KEY(`id`),
	CONSTRAINT `saadi_external_company_study_unique` UNIQUE(`organizationId`,`studyId`)
);
--> statement-breakpoint
CREATE TABLE `saadiFinancialHistoricalData` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int,
	`studyId` int NOT NULL,
	`periodYear` int NOT NULL,
	`section` enum('DRE','BALANCO','FLUXO_CAIXA') NOT NULL,
	`metric` varchar(120) NOT NULL,
	`value` varchar(40) NOT NULL,
	`currency` varchar(3) NOT NULL DEFAULT 'AOA',
	`sourceDocumentId` int,
	`sourcePage` int,
	`sourceField` varchar(120),
	`dataOrigin` enum('ERP','DOCUMENTO','MANUAL','IA') NOT NULL,
	`validationStatus` enum('PENDENTE','VALIDADO','REJEITADO') NOT NULL DEFAULT 'PENDENTE',
	`validatedBy` int,
	`validatedAt` timestamp,
	`valueHash` varchar(64) NOT NULL,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `saadiFinancialHistoricalData_id` PRIMARY KEY(`id`),
	CONSTRAINT `saadi_historical_metric_unique` UNIQUE(`studyId`,`periodYear`,`section`,`metric`)
);
--> statement-breakpoint
CREATE TABLE `saadiFinancingSources` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int,
	`studyId` int NOT NULL,
	`sourceType` enum('CAPITAL_PROPRIO','EMPRESTIMO_BANCARIO','INVESTIDOR','SUBSIDIO','LEASING','OUTRO') NOT NULL,
	`description` varchar(180) NOT NULL,
	`amount` varchar(40) NOT NULL,
	`interestRate` varchar(40) NOT NULL DEFAULT '0',
	`termMonths` int NOT NULL DEFAULT 0,
	`graceMonths` int NOT NULL DEFAULT 0,
	`periodicity` varchar(40) NOT NULL DEFAULT 'MENSAL',
	`startDate` timestamp,
	`endDate` timestamp,
	`guarantees` text,
	`commissions` varchar(40) NOT NULL DEFAULT '0',
	`currency` varchar(3) NOT NULL DEFAULT 'AOA',
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `saadiFinancingSources_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `saadiInvestmentItems` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int,
	`studyId` int NOT NULL,
	`description` varchar(180) NOT NULL,
	`quantity` varchar(40) NOT NULL DEFAULT '1',
	`unitValue` varchar(40) NOT NULL,
	`totalValue` varchar(40) NOT NULL,
	`category` varchar(80) NOT NULL,
	`expectedDate` timestamp,
	`sourceDocumentId` int,
	`currency` varchar(3) NOT NULL DEFAULT 'AOA',
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `saadiInvestmentItems_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `saadiProjections` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int,
	`studyId` int NOT NULL,
	`scenarioId` int,
	`periodYear` int NOT NULL,
	`metric` varchar(120) NOT NULL,
	`value` varchar(40) NOT NULL,
	`currency` varchar(3) NOT NULL DEFAULT 'AOA',
	`formulaVersion` varchar(40) NOT NULL DEFAULT 'v1',
	`sourceHash` varchar(64) NOT NULL,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `saadiProjections_id` PRIMARY KEY(`id`),
	CONSTRAINT `saadi_projection_metric_unique` UNIQUE(`studyId`,`scenarioId`,`periodYear`,`metric`)
);
--> statement-breakpoint
CREATE TABLE `saadiStudyDocuments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int,
	`studyId` int NOT NULL,
	`filename` varchar(255) NOT NULL,
	`mimeType` varchar(120) NOT NULL,
	`size` int NOT NULL,
	`storageKey` varchar(500) NOT NULL,
	`sha256` varchar(64) NOT NULL,
	`category` varchar(80) NOT NULL,
	`sourceLabel` varchar(255),
	`extractionStatus` enum('NAO_INICIADA','EXTRAIDO','EM_REVISAO','VALIDADO','REJEITADO') NOT NULL DEFAULT 'NAO_INICIADA',
	`extractionJson` text,
	`validationNotes` text,
	`validatedBy` int,
	`validatedAt` timestamp,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `saadiStudyDocuments_id` PRIMARY KEY(`id`),
	CONSTRAINT `saadi_study_document_hash_unique` UNIQUE(`studyId`,`sha256`)
);
--> statement-breakpoint
CREATE TABLE `saadiValidations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`organizationId` int NOT NULL,
	`companyId` int NOT NULL,
	`studyId` int NOT NULL,
	`requirementCode` varchar(100) NOT NULL,
	`status` enum('PENDENTE','VALIDADO','BLOQUEADO') NOT NULL,
	`message` varchar(500) NOT NULL,
	`checkedBy` int,
	`checkedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `saadiValidations_id` PRIMARY KEY(`id`),
	CONSTRAINT `saadi_validation_requirement_unique` UNIQUE(`studyId`,`requirementCode`)
);
